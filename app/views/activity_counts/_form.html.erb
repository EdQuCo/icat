<%= form_for(@activity_count) do |f| %>
  <% if @activity_count.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_count.errors.count, "error") %> prohibited this activity_count from being saved:</h2>

      <ul>
      <% @activity_count.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
